export default function Navbar() {
  return (
    <nav>
      <h2>ExpenseTracker</h2>

      <div>
        <span>Dashboard</span>
        <span>Expenses</span>
        <span>Profile</span>
      </div>
    </nav>
  );
}